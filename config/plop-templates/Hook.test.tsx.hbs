import { renderHook } from "@testing-library/react"; import
{{camelCase name}}
from "./{{camelCase name}}"; describe("Hooks /
{{camelCase name}}", () => { it("should render", () => { const { result } =
renderHook(() =>
{{camelCase name}}()); expect(result.current).toBeTruthy(); }); });